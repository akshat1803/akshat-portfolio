# Akshat Sharma — Portfolio

A responsive single-page portfolio built with React, TypeScript, and Vite.

## Run locally

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
npm run preview
```

The production output is generated in `dist/` and can be deployed to Vercel, Netlify, Cloudflare Pages, or any static host.
